# 来源

本机这份 `dsh-process-pin` 没有指向外部作者的仓库地址。

- 许可证：MIT。版权声明在 `LICENSE`：Copyright (c) 2026 dsh-process-pin contributors
- 本地版本号：`0.1.7-local.1`
- 接到 DeepSeek Harness `0.1.7-rc.1` 的改动：去掉 `settings.register` 和 `settingsScope`，偏好改写在插件自己的 `Config` 里，浏览器通过 `configForms` 读写；设置页从已经不存在的 `settings.plugin.item` 改挂到 `settings.plugins.tab`

原 `LICENSE` 没有改，也没有把本机账户写成作者。
